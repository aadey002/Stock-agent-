name: stock-agent-env
channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.11
  - pandas
  - numpy
  - scipy
  - matplotlib
  - pip
  - pip:
      - yfinance
      - requests
      - python-dateutil
      - schedule
      - ta
      - tqdm
