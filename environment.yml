name: confluence-agent
channels:
  - conda-forge
  - defaults

dependencies:
  # Core
  - python=3.11
  - pip

  # (Optional) Common data/analysis stack you might add later
  # - pandas
  # - numpy
  # - requests
  # - matplotlib
  # - openpyxl

  # If you want to manage packages via pip instead of conda, list them here:
  # - pip:
  #   - pandas
  #   - requests

